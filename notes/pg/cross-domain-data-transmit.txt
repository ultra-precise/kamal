file:///C:/Users/kamal.sankhla/Desktop/windv/collect.html

---


var popup = window.open("https://www.google.com", "DoNotClose", "width=150,height=50,location=0");
popup.postMessage("sending data..", "https://www.google.com");

function receiveMessage(event) {
  
  if (event.origin !== "https://www.google.com")
    return;

  // event.source is popup
  // event.data is "receiving data.."
}

window.addEventListener("message", receiveMessage, false);


//---------------------




function receiveMessage(event) {
 
  if (event.origin !== "https://www.bing.com")
    return;

  // event.source is window.opener
  // event.data is "sending data.."

  event.source.postMessage("receiving data..", event.origin);
}

window.addEventListener("message", receiveMessage, false);
